import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.interativixbot',
  appName: 'interativixbot',
  webDir: 'dist'
};

export default config;